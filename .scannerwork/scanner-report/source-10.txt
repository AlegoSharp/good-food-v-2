<ion-header>
  <ion-toolbar>
    <ion-title>{{title}}</ion-title>
  </ion-toolbar>
</ion-header>

<ion-content>
  <div>
    <ion-searchbar color="dark">

    </ion-searchbar>
    <ion-card class="objectCard" *ngFor="let obj of Objects" color="light" (click)="selectObject(obj)">
      <div *ngFor="let item of MyForm.properties">
        <div *ngIf="!item.nom.includes('id') && item.type !== 'boolean' && !item.nom.includes('url')">
          <h5>
            <ion-label color="success">
              {{item.nom.toUpperCase()}}
            </ion-label>
          </h5>
          <ion-label>
            {{obj[item.nom].toString().toUpperCase()}}
          </ion-label>
        </div>
        <div *ngIf="item.nom.includes('url')" style="width: 75px; height: auto; margin: 0 auto;">
          <img src="{{obj[item.nom].toString()}}"/>
        </div>
      </div>
    </ion-card>
  </div>

</ion-content>
